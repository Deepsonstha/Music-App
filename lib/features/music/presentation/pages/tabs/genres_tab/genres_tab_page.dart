import 'package:flutter/material.dart';

class GenresTabPage extends StatelessWidget {
  const GenresTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
