import 'package:flutter/material.dart';

class FullScreenSongPlay extends StatelessWidget {
  const FullScreenSongPlay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
